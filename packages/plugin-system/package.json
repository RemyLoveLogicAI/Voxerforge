{
  "name": "@voxforge/plugin-system",
  "version": "0.1.0",
  "description": "Plugin system for VoxForge IDE extensibility",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "keywords": ["voxforge", "plugin", "extensibility"],
  "license": "MIT",
  "dependencies": {
    "@voxforge/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
