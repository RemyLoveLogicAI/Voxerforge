{
  "name": "@voxforge/rag-engine",
  "version": "0.1.0",
  "description": "Retrieval Augmented Generation engine for code intelligence",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "keywords": ["voxforge", "rag", "embeddings"],
  "license": "MIT",
  "dependencies": {
    "@voxforge/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
